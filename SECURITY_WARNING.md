# ⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ БЕЗОПАСНОСТИ

## Обнаружены утечки креденшелов в истории Git

В истории репозитория были найдены следующие чувствительные данные:

1. **API токен доступа** в файле `.whoop_api_token` (коммит adad4b5)
2. **OAuth Client ID и Client Secret** в файлах:
   - `whoop_obsidian/auth_helper.py`
   - `OAUTH_SETUP.md`

## 🔴 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ ТРЕБУЮТСЯ

### 1. Ротация креденшелов

**СРОЧНО сделайте следующее:**

1. Зайдите в https://developer.whoop.com/
2. Удалите текущее OAuth приложение или сгенерируйте новые Client ID/Secret
3. Создайте новое приложение с новыми креденшелами
4. Получите новый API токен

### 2. Очистка истории Git (ОПАСНО!)

**ВНИМАНИЕ:** Эта операция перепишет историю Git. Все, кто клонировал репозиторий, должны будут сделать `git pull --rebase` или переклонировать.

```bash
# Опция 1: Используйте git-filter-repo (рекомендуется)
# Установка: brew install git-filter-repo

git filter-repo --path .whoop_api_token --invert-paths
git filter-repo --replace-text <(echo 'regex:8b1dd617-41e6-4e3a-bca3-[a-f0-9-]+==>***REMOVED***')
git filter-repo --replace-text <(echo 'regex:d221daaa4edeede3bab128edf65a5e72816a4c1ed9ab48cda4421127c70fb646==>***REMOVED***')
git filter-repo --replace-text <(echo 'regex:LSjFLLSpVsf6JaDN.*==>***REMOVED***')
git filter-repo --replace-text <(echo 'regex:kNo-VTs09-EEQIF.*==>***REMOVED***')

# Опция 2: Используйте BFG Repo-Cleaner
# Установка: brew install bfg
bfg --delete-files .whoop_api_token
bfg --replace-text replacements.txt  # создайте файл с паттернами
```

### 3. Force push (ОПАСНО!)

После очистки истории:

```bash
git push origin --force --all
git push origin --force --tags
```

### 4. Если репозиторий публичный

Если этот репозиторий **когда-либо был публичным** на GitHub:

1. **Креденшелы скомпрометированы безвозвратно** - даже после удаления из истории
2. **ОБЯЗАТЕЛЬНО** сделайте ротацию всех креденшелов
3. Рассмотрите возможность удаления и пересоздания репозитория

## ✅ Исправления уже применены

Следующие изменения уже внесены для предотвращения будущих утечек:

1. ✅ `.whoop_api_token` добавлен в `.gitignore`
2. ✅ Файл удален из индекса git (`git rm --cached`)
3. ✅ Хардкоженные креденшелы удалены из `auth_helper.py`
4. ✅ Креденшелы удалены из `OAUTH_SETUP.md`
5. ✅ Код изменен для использования переменных окружения

## 📋 Проверочный список

- [ ] Сгенерированы новые OAuth Client ID и Client Secret
- [ ] Получен новый API токен
- [ ] Очищена история Git (опционально, но рекомендуется)
- [ ] Force push выполнен (если очищена история)
- [ ] Все члены команды уведомлены о смене креденшелов
- [ ] Локальные `.whoop_api_token` файлы обновлены новым токеном
- [ ] Переменные окружения обновлены новыми значениями

## 🔒 Рекомендации на будущее

1. **Никогда не коммитьте креденшелы** в git
2. Используйте `.env` файлы с `.gitignore`
3. Используйте переменные окружения для всех секретов
4. Используйте pre-commit hooks для проверки секретов (например, `detect-secrets`)
5. Регулярно ротируйте API токены
6. Используйте менеджеры секретов (1Password, AWS Secrets Manager и т.д.)

## Дополнительная информация

- [GitHub: Removing sensitive data](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [git-filter-repo documentation](https://github.com/newren/git-filter-repo)
- [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/)
